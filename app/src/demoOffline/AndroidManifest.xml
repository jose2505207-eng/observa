<?xml version="1.0" encoding="utf-8"?>
<!--
  demoOffline flavor: physically no network. Strips INTERNET / ACCESS_NETWORK_STATE that ML Kit and
  some transitive libs declare. Proves airplane-mode runtime: detection, OCR, navigation (GPS+compass),
  and translation of ALREADY-INSTALLED language models all work with no network.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" tools:node="remove" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" tools:node="remove" />
</manifest>
