<?xml version="1.0" encoding="utf-8"?>

<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CAMERA" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <!-- GPS Orientation Lite: device location for heading/bearing/distance guidance. GPS is
         satellite-based and works offline; this is NOT network/INTERNET access. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!--
      OBSERVA is offline by design. INTERNET / ACCESS_NETWORK_STATE handling is per-flavor:
        * demoOffline (default): src/demoOffline/AndroidManifest.xml STRIPS them — the app
          physically cannot use the network (airplane-mode proof). OCR + translation of
          already-installed models still run fully on device.
        * provisioning: src/provisioning/AndroidManifest.xml ADDS INTERNET so maps and
          ML Kit language packs can be downloaded once, then used offline.
    -->

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.OBSERVA">

        <!-- THE fix that enables QNN/NPU on Android 12+: declare the vendor cDSP FastRPC client so the
             app's linker namespace can dlopen libcdsprpc.so. Without this, QNN HTP fails to create the
             FastRPC transport to the Hexagon cDSP ("Failed to create transport for device, error: 4000"
             → "Failed to load skel, error: 4000") and falls back to XNNPACK CPU. With it, the v79 skel
             loads on the cDSP and the detector runs on the NPU (~2 ms vs ~30 ms CPU). required=false so
             the app still installs on devices without the library (it falls back to XNNPACK CPU).
             Ref: github.com/psiddh/executorch pr-20057 examples/qualcomm/qnn-htp-test. -->
        <uses-native-library
            android:name="libcdsprpc.so"
            android:required="false" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.OBSERVA"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".service.AmbientAwarenessService"
            android:exported="false"
            android:foregroundServiceType="camera" />
    </application>

</manifest>